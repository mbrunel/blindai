version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.9"
  jobs:
    pre_install:
    - ls

mkdocs:
  configuration: mkdocs.yml

python:
   install:
   - requirements: docs/requirements.txt
  